<html lang="en">
<head>
    <%- include('../head') %>
    <title>Document</title>
</head>
<body>
    <%- include('nav') %>
     <div class="container my-5">
        <a href="javascript:history.back()">
                        <i class="bi bi-arrow-left"></i>
                    </a>
        <h1 class="mb-4 text-center">Ledger</h1>

        <% if (entries.length === 0) { %>
            <div class="alert alert-info text-center">No journal entries found.</div>
        <% } else { %>
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Date</th>
                            <th>Reference</th>
                            <th>Debit (Rs)</th>
                            <th>Credit (Rs)</th>
                            <th>Balance (Rs)</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% let totalDebit = 0; let totalCredit = 0; let totalBalance = 0; %>
                        <% entries.forEach(entry => { %>
                            <tr>
                                <td><%= new Date(entry.date).toLocaleDateString() %></td>
                                <td><%= entry.name %></td>
                                <td><%= entry.debit.toFixed(2) %></td>
                                <td><%= entry.credit.toFixed(2) %></td>
                                <td><%= entry.balance.toFixed(2) %></td>
                            </tr>
                            <% totalDebit += entry.debit; totalCredit += entry.credit; totalBalance += entry.balance; %>
                        <% }); %>
                        <tr class="fw-bold bg-light">
                            <td colspan="2" class="text-end">Total:</td>
                            <td><%= totalDebit.toFixed(2) %></td>
                            <td><%= totalCredit.toFixed(2) %></td>
                            <td><%= totalBalance.toFixed(2) %></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        <% } %>
    </div>

</body>
</html>