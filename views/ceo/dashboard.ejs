<!DOCTYPE html>
<html>

<head>
    <%- include('../head') %>
        <title>CEO Dashboard</title>
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
        <style>
            .metric-card {
                border-left: 3px solid #7C4DFF;
            }
        </style>
</head>

<body>

    <div class="container-fluid">
        <div class="row position-sticky">

            <%- include('../sidebar') %>

                <main class="col-md-9 col-lg-10 p-4">
                    <div class="d-flex justify-content-between align-items-center">
                        <a href="javascript:history.back()">

                            <i class="bi bi-arrow-left"></i>

                        </a>
                        <div>
                            <p class="custom-card-header" style="font-size: 23px;">Hello, <%= user.full_name[0].toUpperCase() +
                                    user.full_name.slice(1) %>
                            </p>
                        </div>
                        <h6  onclick="toggleNotifications()">
                        🔔 
                        <span id="notifCount" class="badge bg-danger">0</span>
                        </h6>
                    </div>


                    <h3 class="mb-4">CEO Dashboard</h3>

                    <!-- Financial Cards -->

                    <div class="row g-4 mb-4">

                        <div class="col-md-3">
                            <div class="custom-card metric-card p-3">
                                <div>
                                    <h6 class="metric-title">Revenue</h6>
                                    <h4>PKR <%= Number(revenue).toLocaleString() %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="custom-card metric-card p-3">
                                <div>
                                    <h6 class="metric-title">Expenses</h6>
                                    <h4>PKR <%= Number(expenses).toLocaleString() %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="custom-card metric-card p-3">
                                <div>
                                    <h6 class="metric-title">Net Monthly Profit</h6>
                                    <h4>PKR <%= Number(profit).toLocaleString() %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="metric-card custom-card p-3">
                                <div>
                                    <h6 class="metric-title">Profit Margin</h6>
                                    <h4>
                                        <%= revenue ? ((profit/revenue)*100).toFixed(1) : 0 %>%
                                    </h4>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- KPI metric-card custom-cards -->

                    <div class="row g-4 mb-4">

                        <div class="col-md-3">
                            <div class="metric-card custom-card p-3 ">
                                <div>
                                    <h6 class="metric-title">Customers</h6>

                                    <h4>
                                        <%= customers %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="metric-card custom-card p-3">
                                <div>
                                    <h6 class="metric-title">Vendors</h6>

                                    <h4>
                                        <%= vendors %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="metric-card custom-card p-3 ">

                                <div>
                                    <h6 class="metric-title">Products</h6>
                                    <h4>
                                        <%= products %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="metric-card custom-card p-3 ">
                                <div>
                                    <h6 class="metric-title">Orders</h6>


                                    <h4>
                                        <%= orders %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                    </div>

                    <!-- Risk Indicators -->

                    <div class="row g-4 mb-4">

                        <div class="col-md-3">
                            <div class="metric-card custom-card p-3">
                                <div>
                                    <h6 class="metric-title">Receivables</h6>

                                    <h4>PKR <%= Number(receivables).toLocaleString() %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="metric-card custom-card p-3">

                                <div>
                                    <h6 class="metric-title">Payables</h6>

                                    <h4>PKR <%= Number(payables).toLocaleString() %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="metric-card custom-card p-3">

                                <div>
                                    <h6 class="metric-title">Low Stock</h6>

                                    <h4>
                                        <%= lowStock %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="metric-card custom-card p-3">
                                <div>
                                    <h6 class="metric-title">Pending complaints</h6>

                                    <h4>
                                        <%= complaints %>
                                    </h4>
                                </div>
                            </div>
                        </div>

                    </div>

                    <!-- Sales vs Purchases Chart -->
                    <div class="row mb-4">

                        <!-- Sales vs Purchases Chart -->
                        <div class="col-md-6">
                            <div class="custom-card p-4 mb-4">
                                <h5>Sales vs Purchases</h5>
                                <div style="height: 300px; width: 100%;">
                                    <canvas id="salesChart"></canvas>
                                </div>
                            </div>
                        </div>

                        <!-- Profits Chart -->
                        <div class="col-md-6">
                            <div class="custom-card p-4 mb-4">
                                <h5>Profits</h5>
                                <div style="height: 300px; width: 100%;">
                                    <canvas id="profitChart" height="150"></canvas>
                                </div>
                            </div>
                        </div>

                    </div>

                    <!-- Top Tables -->

                    <div class="row">

                        <div class="col-md-4 mb-3">
                            <div class="custom-card p-3">
                                <h6>Top Customers</h6>
                                <table class="table">
                                    <% topCustomers.forEach(c=>{ %>
                                        <tr>
                                            <td>
                                                <%= c.name %>
                                            </td>
                                            <td>
                                                <%= Number(c.revenue).toLocaleString() %>
                                            </td>
                                        </tr>
                                        <% }) %>
                                </table>
                            </div>
                        </div>

                        <div class="col-md-4 mb-3">
                            <div class="custom-card p-3">
                                <h6>Top Selling Products</h6>
                                <table class="table">
                                    <% topProducts.forEach(p=>{ %>
                                        <tr>
                                            <td>
                                                <%= p.name %>
                                            </td>
                                            <td>
                                                <%= p.sold %>
                                            </td>
                                        </tr>
                                        <% }) %>
                                </table>
                            </div>
                        </div>

                        <div class="col-md-4 mb-3">
                            <div class="custom-card p-3">
                                <h6>Top Vendors</h6>
                                <table class="table">
                                    <% topVendors.forEach(v=>{ %>
                                        <tr>
                                            <td>
                                                <%= v.name %>
                                            </td>
                                            <td>
                                                <%= Number(v.purchases).toLocaleString() %>
                                            </td>
                                        </tr>
                                        <% }) %>
                                </table>
                            </div>
                        </div>

                    </div>
                </main>
        </div>
    </div>
      <div id="notificationSidebar" class="notification-sidebar">
        <div class="sidebar-header">
        <h5>Notifications</h5>
        <button class="btn btn-sm btn-danger"
                onclick="toggleNotifications()">
            ✖
        </button>
    </div>

    <div class="sidebar-body">

        <% if (notifications.length === 0) { %>

            <p class="text-muted">No notifications</p>

        <% } else { %>

            <% notifications.forEach(n => { %>

                <div class="notification-item">

                    <a  href="/admin/jobs/all" onclick="markAsRead(<%= n.id %>)">

                        <strong>
                            <%= n.title %>
                        </strong>

                        <p>
                            <%= n.message %>
                        </p>

                        <small class="text-muted">
                            <%= n.created_at %>
                        </small>

                    </a>

                </div>

            <% }) %>

        <% } %>

    </div>
</div>

    <script>
        document.addEventListener('DOMContentLoaded', async () => {

            const userRole = "<%= user.role_id || '' %>";
            if (userRole !== '1') return;

            const res = await fetch("/procure/manager/new-updates");
            const data = await res.json();
            const name = <%- JSON.stringify(user.full_name) %>;

            const synth = window.speechSynthesis;
            let selectedVoice;

            function loadVoices() {
                const voices = synth.getVoices();
                selectedVoice = voices.find(v => v.lang === "en-US") || voices[0];
            }

            loadVoices();
            speechSynthesis.onvoiceschanged = loadVoices;

            function speak(text, callback) {
                const speech = new SpeechSynthesisUtterance(text);
                speech.voice = selectedVoice;
                speech.lang = "en-US";
                speech.rate = 1;

                if (callback) speech.onend = callback;

                synth.speak(speech);
            }

            setTimeout(() => {

                speak(
                    `Hello ${name}. ${data.message}.`
                );

            }, 1000);


            function listenForResponse() {

                const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
                if (!SpeechRecognition) return;

                const recognition = new SpeechRecognition();
                recognition.lang = 'en-US';

                recognition.onresult = (event) => {

                    const result = event.results[0][0].transcript.toLowerCase();
                    recognition.stop();

                    if (result.includes("yes")) {

                        speak(data.message);

                    } else {

                        speak("Okay. Let me know if you need anything.");

                    }

                };

                recognition.start();
            }

        });
    </script>
    <script>

        const chartData = <%- JSON.stringify(chart) %>;

        const labels = chartData.map(x => x.month);
        const sales = chartData.map(x => x.sales);
        const purchases = chartData.map(x => x.purchases);

        new Chart(document.getElementById("salesChart"), {
            type: "bar",
            data: {
                labels: labels,
                datasets: [
                    {
                        label: "Sales",
                        data: sales
                    },
                    {
                        label: "Purchases",
                        data: purchases
                    }
                ]
            }
        });

        const profitData = <%- JSON.stringify(profits) %>;
        const profitsValues = profitData.map(x => x.profit);
        const salesValues = profitData.map(x => x.sales_revenue);
        const expenseValues = profitData.map(x => x.expense);
        const profitLabels = profitData.map(x => {
        const monthNames = [
            "", "Jan", "Feb", "Mar", "Apr", "May", "Jun",
            "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"
        ];
        return `${monthNames[x.month]} ${x.year}`;
    });
        new Chart(document.getElementById("profitChart"), {
            type: "line",
            data: {
                labels: profitLabels,
                datasets: [{
                    label: "Profit",
                    data: profitsValues,
                    fill: false, // fill under the line
                    borderColor: "#7C4DFF",
                    backgroundColor: "#7C4DFF26",
                    tension: 0.3 // smooth curve
                },
                  {
                    label: "Sales Revenue",
                    data: salesValues,
                    borderColor: "#28a745",
                    backgroundColor: "#28a74526",
                    fill: false,
                    tension: 0.3
                },
                {
                    label: "Expense",
                    data: expenseValues,
                    borderColor: "#dc3545",
                    backgroundColor: "#dc354526",
                    fill: false,
                    tension: 0.3
                },
            ]
            },
            options: {
                responsive: true,
                plugins: { legend: { position: "top" } },
                scales: {
                    y: { beginAtZero: true },
                    x: { ticks: { maxRotation: 0 } }
                }
            }
        });

    function toggleNotifications() {

    const sidebar =
        document.getElementById("notificationSidebar");

    sidebar.classList.toggle("open");
}
async function markAsRead(id) {

    await fetch(
        "/admin/notifications/mark-read/" + id
    );

    loadNotificationCount();
}

    async function loadNotificationCount() {
    try {
        const res = await fetch("/admin/notifications");
        const data = await res.json();

        document.getElementById("notifCount").innerText = data.count;
    } catch (err) {
        console.error(err);
    }
    }

    // Run immediately
    loadNotificationCount();

    // Then repeat
    setInterval(loadNotificationCount, 10000);
    </script>
</body>

</html>